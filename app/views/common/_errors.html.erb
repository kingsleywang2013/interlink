<% if obj.errors.any? %>
  <div class="panel panel-danger">
    <div class="panel-heading">
      <%= pluralize(obj.errors.count, 'error') %> prevented from creating job.
    </div>
    <div class="panel-body">
      <ul>
        <% obj.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>>
  </div>

<% end %>
